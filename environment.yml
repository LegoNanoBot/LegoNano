name: legonanobot
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - nodejs>=20
  - pip:
      - -e .[dev]
      - fastapi>=0.115.0
      - uvicorn[standard]>=0.34.0
      - aiosqlite>=0.21.0
      - sse-starlette>=2.2.0
